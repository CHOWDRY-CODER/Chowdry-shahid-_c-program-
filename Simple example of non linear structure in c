#include <stdio.h>
int main() {
int graph[3][3] = {{0, 1, 0}, {1, 0, 1}, {0, 1, 0}}; 
// Adjacency matrix representation of a graph
for(int i = 0; i < 3; i++) {
for(int j = 0; j < 3; j++)
printf(“%d “, graph[i][j]); // Print the 
adjacency matrix
printf(“\n”);
}
return 0;
}
